spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lposca/microservice-config-data
          username: lposca
          password: ${GIT}
          clone-on-start: true
          default-label: main
server:
  port: 8888 